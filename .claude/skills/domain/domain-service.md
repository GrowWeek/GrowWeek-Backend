# 도메인 서비스 도입 결정 기준

1. 그 바운디드 컨텍스트 내의 최상위 도메인 객체 여럿이 엮인 비즈니스 로직이 존재할 때
   - 예: 회고는 회고 질문과 회고 답변 도메인 객체가 엮여서 동작하는데, 이 둘을 조율하는 로직이 필요할 때
2. 그 도메인 로직을 구현하기 위해 작성할 코드 라인이 100줄 이상이 필요하다고 판단될 때.
3. repository 인터페이스의 여러 구현체의 조합이 필요한 로직은 아닐때.
   - 예: 여러 DB에서 데이터를 조회한 후 조합하는 로직 등은 도메인 서비스로 구현하지 않음.